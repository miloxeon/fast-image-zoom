{
	"name": "image-zoom",
	"version": "2.1.0",
	"description": "Image zoom on click as seen on popular publishing platform ",
	"repository": "git@github.com:mvoloskov/image-zoom.git",
	"main": "src/index.js",
	"author": "mvoloskov",
	"license": "BSL-1.0",
	"scripts": {
		"build-iife": "rollup -c"
	},
	"husky": {
		"hooks": {
			"pre-commit": "npm run build-iife && git add dist"
		}
	},
	"devDependencies": {
		"husky": "^4.2.5",
		"rollup": "^2.26.10"
	}
}
